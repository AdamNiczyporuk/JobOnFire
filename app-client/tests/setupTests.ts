import '@testing-library/jest-dom';

// Generic mock for api module
jest.mock('@/api', () => ({
  __esModule: true,
  default: {
    get: jest.fn(),
    post: jest.fn(),
    put: jest.fn(),
    delete: jest.fn()
  }
}));
